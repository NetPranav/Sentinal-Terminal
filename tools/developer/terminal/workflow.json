{
  "toolId": "developer.terminal",
  "version": "1.0.0",
  "description": "Executes native driver for Launch Standalone Terminal.",
  "steps": [
    {
      "id": "step_exec_main",
      "name": "Execute Launch Standalone Terminal",
      "type": "ExecuteCapability",
      "capabilityId": "developer.terminal",
      "parameters": {
        "path": "{{path}}"
      },
      "dependencies": [],
      "onError": "fail"
    }
  ],
  "successCondition": "step:step_exec_main:success",
  "errorHandling": {
    "strategy": "fail_fast",
    "maxRetries": 0
  }
}
