{
  "toolId": "developer.github",
  "version": "1.0.0",
  "description": "Executes native driver for Interact with GitHub CLI.",
  "steps": [
    {
      "id": "step_exec_main",
      "name": "Execute Interact with GitHub CLI",
      "type": "ExecuteCapability",
      "capabilityId": "developer.github",
      "parameters": {
        "command": "{{command}}"
      },
      "dependencies": [],
      "onError": "fail"
    }
  ],
  "successCondition": "step:step_exec_main:success",
  "errorHandling": {
    "strategy": "fail_fast",
    "maxRetries": 0
  }
}
