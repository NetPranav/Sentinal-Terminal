{
  "toolId": "git.pull",
  "version": "1.0.0",
  "description": "Executes native driver for Pull Remote Git Changes.",
  "steps": [
    {
      "id": "step_exec_main",
      "name": "Execute Pull Remote Git Changes",
      "type": "ExecuteCapability",
      "capabilityId": "git.pull",
      "parameters": {
        "remote": "{{remote}}",
        "branch": "{{branch}}"
      },
      "dependencies": [],
      "onError": "fail"
    }
  ],
  "successCondition": "step:step_exec_main:success",
  "errorHandling": {
    "strategy": "fail_fast",
    "maxRetries": 0
  }
}
