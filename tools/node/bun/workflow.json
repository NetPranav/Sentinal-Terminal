{
  "toolId": "node.bun",
  "version": "1.0.0",
  "description": "Executes native driver for Execute via Bun Runtime.",
  "steps": [
    {
      "id": "step_exec_main",
      "name": "Execute Execute via Bun Runtime",
      "type": "ExecuteCapability",
      "capabilityId": "node.bun",
      "parameters": {
        "command": "{{command}}"
      },
      "dependencies": [],
      "onError": "fail"
    }
  ],
  "successCondition": "step:step_exec_main:success",
  "errorHandling": {
    "strategy": "fail_fast",
    "maxRetries": 0
  }
}
