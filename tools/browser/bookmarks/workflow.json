{
  "toolId": "browser.bookmarks",
  "version": "1.0.0",
  "description": "Executes native driver for Inspect Browser Bookmarks.",
  "steps": [
    {
      "id": "step_exec_main",
      "name": "Execute Inspect Browser Bookmarks",
      "type": "ExecuteCapability",
      "capabilityId": "browser.bookmarks",
      "parameters": {
        "filter": "{{filter}}"
      },
      "dependencies": [],
      "onError": "fail"
    }
  ],
  "successCondition": "step:step_exec_main:success",
  "errorHandling": {
    "strategy": "fail_fast",
    "maxRetries": 0
  }
}
