{
  "toolId": "filesystem.search",
  "examples": [
    {
      "input": "search for *.ts files in src directory",
      "expectedIntent": {
        "domain": "filesystem",
        "action": "search"
      },
      "expectedEntities": {
        "dir": "sample_val",
        "pattern": "sample_val"
      },
      "description": "Example execution of Search Filesystem by Name"
    }
  ]
}
