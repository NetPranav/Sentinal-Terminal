{
  "toolId": "filesystem.mkdir",
  "version": "1.0.0",
  "description": "Executes native driver for Create New Directory.",
  "steps": [
    {
      "id": "step_exec_main",
      "name": "Execute Create New Directory",
      "type": "ExecuteCapability",
      "capabilityId": "filesystem.mkdir",
      "parameters": {
        "path": "{{path}}"
      },
      "dependencies": [],
      "onError": "fail"
    }
  ],
  "successCondition": "step:step_exec_main:success",
  "errorHandling": {
    "strategy": "fail_fast",
    "maxRetries": 0
  }
}
