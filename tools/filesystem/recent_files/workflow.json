{
  "toolId": "filesystem.recent_files",
  "version": "1.0.0",
  "description": "Executes native driver for Query Recently Modified Files.",
  "steps": [
    {
      "id": "step_exec_main",
      "name": "Execute Query Recently Modified Files",
      "type": "ExecuteCapability",
      "capabilityId": "filesystem.recent_files",
      "parameters": {
        "count": "{{count}}"
      },
      "dependencies": [],
      "onError": "fail"
    }
  ],
  "successCondition": "step:step_exec_main:success",
  "errorHandling": {
    "strategy": "fail_fast",
    "maxRetries": 0
  }
}
